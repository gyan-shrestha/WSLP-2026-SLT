{
  "_note": "VIDEO: SigLIP-base embeddings, 16 frames x 768-d per clip, replacing 103 MediaPipe keypoints. Tests whether a modern visual encoder transfers across the signer gap where pose does not: our dev BLEU rose 0.0073->0.0188 while test BLEU stayed at exactly 0.00 through every pose-side change. downsample=false because 16 frames is already short. clip_value=0 because SigLIP activations are not bounded like normalised keypoints.",
  "model_name": "google/flan-t5-base",
  "feat_dir": "${SLT_ROOT}/vfeats/isign",
  "text_csv": "${SLT_ROOT}/isign/iSign_v1.1.csv",
  "out_dir": "${SLT_ROOT}/runs/video",
  "max_frames": 16,
  "max_target_len": 48,
  "dev_frac": 0.02,
  "batch_size": 96,
  "eval_batch_size": 32,
  "grad_accum": 1,
  "epochs": 14,
  "lr": 3e-05,
  "weight_decay": 0.01,
  "warmup_frac": 0.08,
  "clip": 1.0,
  "dropout": 0.1,
  "num_workers": 12,
  "log_every": 50,
  "eval_batches": 20,
  "seed": 0,
  "lr_frontend": 0.0001,
  "contrastive_weight": 0.5,
  "contrastive_temp": 0.07,
  "downsample": false,
  "clip_value": 0
}