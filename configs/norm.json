{
  "_note": "Run 2 recipe on DOMAIN-INVARIANT features (robust clip-level shoulder scale + per-clip z-scoring). Isolates the normalization fix: previously iSign std 1.99 vs test 3.04 with p99.9 3.29 vs 13.9; now both exactly 1.000. Test BLEU was 0.00 against 0.0119 in-domain.",
  "model_name": "google/flan-t5-base",
  "feat_dir": "${SLT_ROOT}/feats/isign",
  "text_csv": "${SLT_ROOT}/isign/iSign_v1.1.csv",
  "out_dir": "${SLT_ROOT}/runs/norm",
  "max_frames": 512,
  "max_target_len": 48,
  "dev_frac": 0.02,
  "batch_size": 64,
  "eval_batch_size": 32,
  "grad_accum": 1,
  "epochs": 14,
  "lr": 3e-05,
  "weight_decay": 0.01,
  "warmup_frac": 0.08,
  "clip": 1.0,
  "dropout": 0.1,
  "num_workers": 12,
  "log_every": 50,
  "eval_batches": 20,
  "seed": 0,
  "lr_frontend": 0.0001,
  "contrastive_weight": 0.5,
  "contrastive_temp": 0.07
}